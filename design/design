
接口/MCP/SKILL调用
现在需要设计一个接口调用 skill调用 MCP调用的配置功能页面 要求1：对于接口/Skill/MCP调用，需要在一个集合组里面进行配置，可以配置多个集合组，并且集合组可以编辑删除新增，集合组里面可以配置接口/Skill/MCP的认证授权方式，认证授权需要集成市面上主流的比如oauth2,jwt,账号&密码或者其他方式 要求2：接口，Skill,MCP都应
  该在集合组里面进行配置，并且可以删除，有草稿版本和发布版本两种，只有发布版本才能被流程页面中流程调用  要求3：流程中调用的时候应该先选择集合组，然后选择集合中中的某个接口/技能/MCP  要求4:配置好接口后，需要能测试，应该提供测试环境验证接口是否配置正确，如果配置错误或者接口调用有问题，给出相应的提示语。其他
  Skill调用测试 MCP调用测试这个没有想好如何设计，请帮忙补充。思考：流程中调用MCP和Skill是否有意义，有意义的话流程中最好的调用方式是什么，如何触发这个调用，是否是通过专门的提示词触发。请根据要求先设计好这个方案，并进行补充思考中的问题，给出方案交给用户确定，解决方案获得用户同意之后，使用前端agent按照前端设
  计原则修改，后端agent按照后端设计原则修改，修完完之后进行测试联调