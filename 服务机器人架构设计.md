# 服务机器人架构设计文档

## 文档说明

本文档基于当前仓库实现和最新架构要求重新整理，重点解决以下两个核心问题：

1. 后端主交互链路必须使用 `Netty` 的 `EventLoop` 处理 WebSocket 的消息、连接事件和异常，并通过 `Handler` 链拆分职责，而不是仅停留在“能建立 WebSocket 连接”的层面。
2. 智能体执行引擎必须具备自动规划、动态读取上下文、判断当前应执行哪条路径和哪个节点的能力，能够把“用户提示词 + 历史上下文 + 当前运行条件”结合起来进行有边界的自主执行。

**版本**: v2.0  
**日期**: 2026-04-20

---

## 目录

1. [重构结论](#1-重构结论)
2. [现状问题与改造目标](#2-现状问题与改造目标)
3. [目标总体架构](#3-目标总体架构)
4. [Netty 网关架构设计](#4-netty-网关架构设计)
5. [智能体规划与执行架构](#5-智能体规划与执行架构)
6. [上下文模型设计](#6-上下文模型设计)
7. [工作流与节点模型设计](#7-工作流与节点模型设计)
8. [通信协议设计](#8-通信协议设计)
9. [数据存储设计](#9-数据存储设计)
10. [可观测性与安全设计](#10-可观测性与安全设计)
11. [落地实施计划](#11-落地实施计划)

---

## 1. 重构结论

### 1.1 架构重构后的核心结论

- 前端到 Java 后端的主链路统一为 `Netty WebSocket Gateway`，由 `EventLoop + ChannelPipeline + Handler` 负责消息收发、连接生命周期、心跳、事件派发和异常处理。
- `Spring WebSocket` 不再承担主业务交互链路，只保留 `HTTP REST` 作为后台管理、配置维护、文件上传和健康检查接口。
- Java 后端负责连接管理、协议处理、会话绑定、执行编排、事件桥接和审计落库。
- Python AI 引擎从“静态节点调度器”升级为“上下文装配器 + 规划器 + 路径解析器 + 节点执行器 + 重规划器”的智能执行内核。
- 智能体的执行模式不再是“固定入口节点顺序跑完”，而是“在工作流图的约束范围内，根据上下文动态判断下一个可执行节点或分支”。

### 1.2 设计原则

1. **连接处理必须事件驱动**  
   WebSocket 的连接建立、消息接收、异常、超时、关闭必须通过 Netty Handler 链处理，不能把所有逻辑堆在单个 Service 中。

2. **执行引擎必须上下文化**  
   智能体任何一次规划都必须依赖完整上下文，而不是只看当前用户一句话。

3. **自主但有边界**  
   智能体可以自动选路、跳过已满足条件的节点、补齐缺失信息、触发重规划，但不能脱离工作流图无限制自由跳转。

4. **协议统一**  
   前端和 Java 网关之间统一使用 `action / ack / event / error` 四类消息信封，便于前端编排器、聊天界面和执行面板共用一套协议。

5. **执行可回放**  
   每一次规划结果、节点决策、条件判断、工具调用和异常都必须有审计记录，支持回放和排障。

---

## 2. 现状问题与改造目标

### 2.1 当前架构的主要问题

| 问题 | 当前表现 | 风险 |
|------|----------|------|
| WebSocket 主链路不够 Netty 化 | 当前同时存在 `ExecutionWebSocketHandler` 和 `NettyGatewayServer`，但主文档仍是“Spring Boot 接入层 + WebSocket”描述，未形成清晰的 Pipeline/Handler 设计 | 后续难以支撑高并发连接、统一异常处理和协议演进 |
| 消息处理未按 Handler 职责拆分 | 消息解析、校验、派发和业务调用仍偏 Service 化 | 协议扩展时容易耦合，连接事件和业务事件边界不清 |
| 执行引擎偏静态流程 | Python 调度器当前是从入口节点开始，按图执行，条件节点能力也偏简单 | 无法满足“自动规划、判断路径、动态执行节点”的要求 |
| 上下文装配不完整 | 当前上下文主要是会话变量、执行变量和运行指标 | 无法充分融合用户提示词、历史对话、节点历史、知识内容和运行条件 |
| 条件判断能力不足 | `ConditionNode` 主要支持简单字段比较 | 复杂业务判断和语义分流能力不足 |
| 规划不可解释 | 当前没有显式的 Planner 输出模型 | 无法记录“为什么走这条路径、为什么跳过那个节点” |

### 2.2 改造目标

#### 目标一：WebSocket 主链路完全事件驱动

- 使用 Netty 原生 `ServerBootstrap`、`NioEventLoopGroup`、`ChannelInitializer`、`ChannelPipeline`
- 使用独立 `Handler` 处理：
  - 握手认证
  - 协议解码
  - 信封校验
  - 会话绑定
  - 心跳保活
  - 动作派发
  - 出站事件编码
  - 异常映射

#### 目标二：智能体具备动态规划和节点决策能力

- 执行前先构造 `PlanningContext`
- 由 `Planner` 根据“用户意图 + 历史上下文 + 当前图结构 + 运行状态”生成执行计划
- 由 `PathResolver` 约束 Planner 只能在合法节点和边上选择
- 由 `NodeReadinessChecker` 判断节点是否可以跳过、执行或等待
- 在执行过程中支持基于新上下文触发 `Replan`

#### 目标三：保留现有仓库的可演进性

- Java 后端继续保留 `Spring Boot` 作为容器和管理面
- Python AI 引擎继续保留现有 `scheduler / nodes / context` 结构，但要拆分出更清晰的规划子模块
- Java 与 Python 的内部通信第一阶段可以继续用 `HTTP + SSE`，但边界要清晰，后续可升级到 `gRPC Streaming`

---

## 3. 目标总体架构

### 3.1 分层架构图

```text
┌──────────────────────────────────────────────────────────────────┐
│                           前端交互层                             │
│  Chat UI  │  Workflow Editor  │  Execution Panel  │  Replay UI  │
└───────────────────────────────┬──────────────────────────────────┘
                                │ WebSocket
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Java Netty Gateway（主交互链路）              │
│                                                                  │
│  EventLoopGroup                                                   │
│    └─ ChannelPipeline                                             │
│       ├─ IdleStateHandler                                         │
│       ├─ HttpServerCodec                                          │
│       ├─ HttpObjectAggregator                                     │
│       ├─ WebSocketServerProtocolHandler                           │
│       ├─ AuthHandshakeHandler                                     │
│       ├─ GatewayEnvelopeDecoder                                   │
│       ├─ SessionBindingHandler                                    │
│       ├─ HeartbeatHandler                                         │
│       ├─ ActionDispatchHandler                                    │
│       ├─ OutboundEventEncoder                                     │
│       └─ GatewayExceptionHandler                                  │
│                                                                  │
│  ConnectionRegistry │ SessionRouter │ ExecutionOrchestrator       │
│  EventBridge        │ AuditLogger   │ BackpressureController      │
└───────────────────────────────┬──────────────────────────────────┘
                                │ HTTP Command + SSE Event Stream
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│                     Python Agent Runtime（执行内核）              │
│                                                                  │
│  ContextAssembler                                                │
│  ContextWindowManager                                            │
│  Planner                                                         │
│  PathResolver                                                    │
│  BranchEvaluator                                                 │
│  NodeReadinessChecker                                            │
│  NodeExecutor                                                    │
│  RePlanner                                                       │
│  WorkflowJournal                                                 │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────────┐
│                         数据与治理层                              │
│  MySQL  │ Redis  │ pgvector / 向量库 │ MinIO │ Metrics / Trace    │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 职责划分

| 层次 | 组件 | 主要职责 |
|------|------|----------|
| 前端 | React + React Flow | 展示聊天、编辑工作流、显示执行事件 |
| Java 网关 | Netty Gateway | 维护长连接、接收 action、推送 event、统一协议和异常 |
| Java 编排层 | ExecutionOrchestrator | 创建执行实例、关联会话、桥接 Python 事件、落库 |
| Python 执行内核 | Planner Loop | 读取上下文、规划路径、执行节点、重规划 |
| 数据层 | MySQL / Redis / Vector DB | 存储工作流、会话、执行日志、上下文快照、缓存 |

### 3.3 技术选型

| 领域 | 技术 | 说明 |
|------|------|------|
| 网关 | Netty 4.1+ | 主 WebSocket 通信和事件循环 |
| Java 容器 | Spring Boot 3.x | 配置管理、REST 管理接口、Bean 生命周期 |
| 前端通信 | WebSocket | 双向消息、执行事件、编排事件 |
| AI 服务 | Python 3.11+ + FastAPI | 执行内核容器 |
| 执行引擎 | 自研 Planner Loop | 基于工作流图的有界自主执行 |
| 持久化 | MySQL | 主数据与审计 |
| 缓存 | Redis | 会话缓存、幂等键、分布式锁 |
| 检索 | pgvector / 向量库 | 知识检索 |
| 监控 | OpenTelemetry + Prometheus + Grafana | 指标、日志、链路追踪 |

---

## 4. Netty 网关架构设计

### 4.1 设计目标

后端网关层必须实现以下能力：

- 统一处理 WebSocket 握手、认证、连接状态、业务消息和异常
- 任何入站消息都由 `Handler` 链依次处理，而不是直接落入 Service
- 任何出站事件都由统一事件编码器发送，保持协议一致
- 保证同一连接上的消息顺序和执行一致性
- 避免阻塞 EventLoop，所有耗时业务调用必须异步转交给业务线程池或反应式链路

### 4.2 线程模型

```text
BossGroup
  └─ 接收连接、完成 TCP/WebSocket 握手

WorkerGroup
  └─ 执行 ChannelPipeline
     ├─ 接收入站帧
     ├─ 解码 JSON
     ├─ 协议校验
     ├─ 连接上下文绑定
     ├─ 将业务命令投递到编排层
     └─ 将出站事件写回 Channel

Business Executor / Reactive Chain
  └─ 执行数据库访问、Python 调用、持久化、审计等耗时操作
```

### 4.3 关键设计约束

1. `EventLoop` 只负责网络相关逻辑和轻量派发  
   不允许在 `channelRead0` 中直接执行耗时数据库查询、模型调用或远程 HTTP 请求。

2. 所有出站写操作必须回到所属 `Channel` 的 `eventLoop()`  
   这样可以保证线程安全和消息顺序。

3. 同一 `session_id` 可以有多个连接，但一个 `execution_id` 的主交互连接必须有明确路由策略  
   默认策略：同 session 下可多端订阅，但表单提交和交互确认必须带 `request_id` 和 `actor_id`。

4. 必须实现背压控制  
   当连接不可写时，不能无限制堆积事件，必须有缓冲阈值、丢弃策略或降级策略。

### 4.4 ChannelPipeline 设计

#### 4.4.1 推荐 Pipeline

```text
ChannelPipeline
  1. IdleStateHandler
  2. HttpServerCodec
  3. HttpObjectAggregator
  4. WebSocketServerProtocolHandler("/ws/robot")
  5. AuthHandshakeHandler
  6. GatewayEnvelopeDecoder
  7. ProtocolValidationHandler
  8. SessionBindingHandler
  9. HeartbeatHandler
 10. RateLimitHandler
 11. ActionDispatchHandler
 12. OutboundEventEncoder
 13. GatewayExceptionHandler
```

#### 4.4.2 Handler 职责说明

| Handler | 职责 |
|---------|------|
| `AuthHandshakeHandler` | 解析 token、workspace、tenant、user 信息，认证失败则拒绝握手 |
| `GatewayEnvelopeDecoder` | 将 TextWebSocketFrame 解码为 `GatewayEnvelope` |
| `ProtocolValidationHandler` | 校验 `type/request_id/action/payload` 字段是否合规 |
| `SessionBindingHandler` | 建立 `channelId ↔ sessionId ↔ executionId ↔ userId` 映射 |
| `HeartbeatHandler` | 处理 ping/pong、空闲连接检测、超时断连 |
| `RateLimitHandler` | 做基础限流、防刷和消息尺寸限制 |
| `ActionDispatchHandler` | 根据 action 路由到具体命令处理器 |
| `OutboundEventEncoder` | 把领域事件编码成统一 JSON 信封并写回前端 |
| `GatewayExceptionHandler` | 捕获 Pipeline 异常，映射为标准 error 包或关闭连接 |

### 4.5 Action Handler 设计

`ActionDispatchHandler` 不直接写业务逻辑，而是继续分发给更细粒度的动作处理器。

#### 4.5.1 推荐动作处理器

| Action | Handler | 说明 |
|--------|---------|------|
| `chat.send` | `ChatActionHandler` | 用户发送消息，启动或续接执行 |
| `form.submit` | `FormSubmitActionHandler` | 提交表单，恢复等待中的执行 |
| `execution.resume` | `ExecutionResumeHandler` | 从挂起状态恢复执行 |
| `execution.suspend` | `ExecutionSuspendHandler` | 人工挂起执行 |
| `workflow.save` | `WorkflowDraftActionHandler` | 保存编排器草稿 |
| `workflow.validate` | `WorkflowValidateActionHandler` | 画布校验 |
| `ping` | `PingActionHandler` | 应用层心跳 |

#### 4.5.2 设计要求

- 每个 Action Handler 只处理一种动作
- 每个 Action Handler 都必须返回 `ack` 或 `error`
- Action Handler 只能发起编排命令，不直接处理底层 Channel 生命周期
- Handler 之间通过统一 `GatewayCommand` 和 `GatewayEvent` 交互

### 4.6 连接生命周期事件

#### 4.6.1 生命周期事件类型

| 事件 | 触发时机 |
|------|----------|
| `connection.opened` | WebSocket 握手完成并认证成功 |
| `connection.bound` | channel 与 session / execution 绑定成功 |
| `connection.idle` | 超过空闲阈值未收到消息 |
| `connection.closed` | 客户端主动断开或服务端关闭 |
| `connection.exception` | Pipeline 出现未恢复异常 |

#### 4.6.2 事件处理规则

- `connection.opened`：写入连接注册表，记录来源 IP、user、workspace
- `connection.bound`：为后续事件推送建立订阅关系
- `connection.idle`：发送 ping；连续超时则主动断开
- `connection.closed`：清理会话映射、缓冲队列和订阅
- `connection.exception`：记录异常类型、请求上下文、最近消息摘要

### 4.7 异常处理设计

#### 4.7.1 异常分类

| 异常类型 | 示例 | 处理方式 |
|----------|------|----------|
| 协议异常 | JSON 非法、字段缺失、类型错误 | 返回 `error`，保留连接 |
| 认证异常 | token 无效、无权限工作空间 | 返回 `error` 后关闭连接 |
| 业务异常 | 无法启动执行、表单状态不匹配 | 返回 `error`，保留连接 |
| 下游异常 | Python 超时、DB 超时、向量检索失败 | 发送 `execution.failed` 或 `execution.degraded` |
| 系统异常 | 未捕获 RuntimeException | 记录错误、返回 `error`，必要时关闭连接 |

#### 4.7.2 标准异常映射

```json
{
  "type": "error",
  "request_id": "req-1001",
  "code": "invalid_payload",
  "message": "payload.message is required",
  "fatal": false,
  "timestamp": "2026-04-20T10:15:12Z"
}
```

### 4.8 与当前仓库的调整建议

#### 4.8.1 Java 网关层重构方向

- `ExecutionWebSocketHandler` 不再作为主业务链路实现
- `NettyGatewayServer` 从当前基于 Reactor Netty 的函数式 `sendWebsocket` 方式，重构为显式的 Netty `ChannelInitializer + Handler Pipeline`
- `GatewayActionService` 由“解析 + 派发一体化”改成：
  - `GatewayEnvelopeDecoder`
  - `ProtocolValidationHandler`
  - `ActionDispatchHandler`
  - 具体的 Action Handler

#### 4.8.2 推荐 Java 包结构

```text
java-backend/src/main/java/robot/agent/
  gateway/
    netty/
      NettyGatewayServer.java
      RobotChannelInitializer.java
    handler/
      AuthHandshakeHandler.java
      GatewayEnvelopeDecoder.java
      ProtocolValidationHandler.java
      SessionBindingHandler.java
      HeartbeatHandler.java
      RateLimitHandler.java
      ActionDispatchHandler.java
      GatewayExceptionHandler.java
    action/
      ChatActionHandler.java
      FormSubmitActionHandler.java
      ExecutionResumeHandler.java
      WorkflowDraftActionHandler.java
    session/
      ConnectionRegistry.java
      SessionChannelRegistry.java
    protocol/
      GatewayEnvelope.java
      GatewayAck.java
      GatewayEvent.java
      GatewayError.java
  execution/
    ExecutionOrchestrator.java
    ExecutionEventBridge.java
    ExecutionAuditService.java
```

---

## 5. 智能体规划与执行架构

### 5.1 设计目标

执行引擎必须支持以下能力：

- 动态读取上下文，而不是只依赖当前节点输入
- 根据用户最新提示词重新判断目标
- 根据当前变量、节点状态、历史结果判断下一步该执行哪个节点
- 对缺失信息自动请求补充，对已有信息自动跳过冗余节点
- 在节点执行结果改变上下文后支持重规划

### 5.2 执行模式：有界自主执行

本系统不采用“无边界自由 Agent”模式，而采用**有界自主执行**：

- 工作流图定义了合法节点集合和合法边集合
- Planner 只能在当前图的候选节点范围内选择
- 当信息不足时，Planner 可以：
  - 进入补充信息节点
  - 触发表单节点
  - 触发澄清消息节点
  - 进入人工兜底节点
- 当信息充分时，Planner 可以：
  - 直接进入目标业务节点
  - 跳过已满足条件的采集节点
  - 根据语义分流选择不同路径

### 5.3 核心组件

| 组件 | 职责 |
|------|------|
| `ContextAssembler` | 装配执行时所需的全量上下文 |
| `ContextWindowManager` | 做长历史裁剪、摘要和上下文窗口控制 |
| `Planner` | 生成当前目标下的执行计划 |
| `PathResolver` | 把 Planner 的结果约束到工作流图内的合法路径 |
| `NodeReadinessChecker` | 判断节点是否可执行、应跳过或需等待 |
| `BranchEvaluator` | 对分支和边条件做规则判断或语义判断 |
| `NodeExecutor` | 真正执行节点 |
| `RePlanner` | 当上下文发生关键变化时触发重规划 |
| `WorkflowJournal` | 记录每一次规划与执行决策 |

### 5.4 规划驱动执行循环

#### 5.4.1 核心执行循环

```text
接收用户消息
  -> 装配 PlanningContext
  -> Planner 输出 Plan
  -> PathResolver 校验合法路径
  -> NodeReadinessChecker 判断节点是否执行/跳过/等待
  -> NodeExecutor 执行节点
  -> 合并节点输出到 ExecutionContext
  -> 判断是否需要 Replan
  -> 继续下一轮，直到完成/失败/等待用户
```

#### 5.4.2 伪代码

```python
while execution.status not in {"completed", "failed", "suspended"}:
    planning_context = context_assembler.build(execution_context, user_input)
    plan = planner.plan(planning_context)
    decision = path_resolver.resolve(plan, workflow_graph, execution_context)

    if decision.need_user_input:
        emit_form_or_clarification(decision)
        wait_user_resume()
        continue

    readiness = node_readiness_checker.check(decision.next_node, execution_context)
    if readiness.should_skip:
        execution_context.mark_skipped(decision.next_node, readiness.reason)
        move_to_next_candidate()
        continue

    result = node_executor.execute(decision.next_node, execution_context)
    execution_context.merge(result.context_delta)
    journal.record(plan, decision, result)

    if replanner.should_replan(result, execution_context):
        continue
```

### 5.5 Planner 的输入与输出

#### 5.5.1 Planner 输入

- 当前用户提示词
- 最近对话窗口
- 历史摘要
- 当前工作流图和当前节点位置
- 会话变量、执行变量、节点输出
- 当前候选节点列表
- 每条边的 guard 条件
- 工具可用性、权限、风控规则
- 知识检索结果摘要

#### 5.5.2 Planner 输出示例

```json
{
  "goal": "为用户完成航班预订",
  "reasoning_summary": "用户已提供出发地和目的地，但缺少出发日期，需要先补齐日期再查询航班",
  "candidate_nodes": ["collect_date", "search_flight", "handoff"],
  "selected_node": "collect_date",
  "selected_edge": "edge_12",
  "need_user_input": true,
  "confidence": 0.93,
  "missing_fields": ["departure_date"]
}
```

### 5.6 NodeReadinessChecker 设计

该组件用于判断“当前节点到底要不要执行”，它直接回应“智能体判断路径节点执行的能力”。

#### 5.6.1 判断规则

| 场景 | 处理策略 |
|------|----------|
| 表单节点所需字段已齐全 | 跳过该节点 |
| 知识节点已有新鲜检索结果 | 可复用缓存并跳过 |
| 工具节点缺少参数 | 进入参数补齐路径 |
| 风控不允许直接调用工具 | 进入确认或人工节点 |
| 当前边条件不满足 | 不允许进入目标节点 |

#### 5.6.2 典型例子

用户说：“帮我预订明天去北京的机票，我从上海出发。”

当前上下文中已存在：

- `departure_city=上海`
- `arrival_city=北京`
- `departure_date=明天`

则智能体不应再进入“收集出发地”和“收集目的地”节点，而应直接选择“查询航班”节点。

### 5.7 BranchEvaluator 设计

`ConditionNode` 不能只支持简单 `equals / contains / greater_than`，需要升级成三层判断机制：

1. **硬规则判断**  
   例如字段非空、数值范围、布尔开关、正则匹配

2. **表达式判断**  
   例如：
   - `order.total_amount > 1000 and user.vip == true`
   - `slot.departure_date is not null and slot.arrival_city is not null`

3. **语义判断**  
   当规则无法覆盖时，允许调用语义分类器或轻量模型做受控判断，但输出必须是结构化结果，并经过合法分支校验

### 5.8 RePlanner 触发条件

以下情况必须触发重规划：

- 用户补充了新的关键信息
- 表单返回改变了缺失槽位状态
- 工具调用失败或返回空结果
- 知识检索结果与原假设冲突
- 风控状态改变，例如工具调用需要人工确认
- 当前候选路径全部不可用

### 5.9 与当前 Python 执行引擎的调整建议

#### 5.9.1 现有问题

当前 `WorkflowScheduler` 已具备节点循环和事件输出能力，但仍偏向“顺着图跑”，缺少显式规划阶段。

#### 5.9.2 推荐拆分

```text
python-ai/src/core/
  context.py
  context_assembler.py
  context_window.py
  planner.py
  path_resolver.py
  branch_evaluator.py
  node_readiness.py
  replanner.py
  scheduler.py
  journal.py
```

#### 5.9.3 演进关系

- `scheduler.py`：保留总循环职责，但不再直接决定下一节点
- `context.py`：扩展为多层上下文模型
- `condition.py`：只保留基础条件能力，复杂判断转移到 `branch_evaluator.py`
- `llm.py`：支持接收规划上下文和结构化约束

---

## 6. 上下文模型设计

### 6.1 上下文分层

智能体不能只维护一个扁平的 `variables` map，而应维护分层上下文。

#### 6.1.1 推荐上下文层次

| 层次 | 内容 |
|------|------|
| `UserPromptContext` | 当前用户输入、附件、结构化表单输入 |
| `ConversationContext` | 最近 N 轮对话内容 |
| `SessionMemory` | 用户长期偏好、已确认事实、业务槽位 |
| `ExecutionContext` | 当前执行实例变量、节点状态、运行指标 |
| `WorkflowContext` | 当前工作流版本、节点图、边条件、可选子流程 |
| `KnowledgeContext` | 检索片段、知识摘要、引用来源 |
| `ToolContext` | 工具可用性、权限、最近工具输出 |
| `GovernanceContext` | 风控规则、确认状态、租户约束 |

### 6.2 ContextAssembler 装配内容

#### 6.2.1 必装配字段

- `current_user_message`
- `recent_messages`
- `conversation_summary`
- `current_node_id`
- `candidate_nodes`
- `session_variables`
- `execution_variables`
- `completed_nodes`
- `failed_nodes`
- `last_tool_results`
- `knowledge_snippets`
- `workflow_goal`
- `active_constraints`
- `security_policies`

#### 6.2.2 装配策略

1. 读取最近对话窗口  
2. 对超过窗口的历史做摘要  
3. 加载会话级记忆和执行级变量  
4. 计算当前可达节点和候选边  
5. 根据当前目标检索相关知识  
6. 合并工具结果和治理规则  
7. 输出 `PlanningContext`

### 6.3 ContextWindowManager 设计

#### 6.3.1 目标

- 控制 prompt token 长度
- 保留对规划最关键的信息
- 避免简单拼接上下文导致噪声过大

#### 6.3.2 策略

| 数据类型 | 策略 |
|----------|------|
| 最近对话 | 保留最近 6~12 轮 |
| 历史长对话 | 压缩成摘要 |
| 节点日志 | 仅保留关键节点和最近节点 |
| 知识片段 | 只保留与当前目标最相关 Top-K |
| 工具输出 | 优先保留结构化字段，丢弃冗长原始文本 |

### 6.4 推荐上下文对象模型

```json
{
  "execution_id": "exec_001",
  "session_id": "sess_001",
  "user_prompt": "帮我订明天从上海到北京的机票",
  "conversation": {
    "recent_messages": [
      {"role": "user", "content": "帮我订明天从上海到北京的机票"}
    ],
    "summary": "用户倾向于优先选择早班航班"
  },
  "memory": {
    "confirmed_slots": {
      "departure_city": "上海",
      "arrival_city": "北京",
      "departure_date": "2026-04-21"
    }
  },
  "workflow": {
    "workflow_code": "flight_booking",
    "current_node_id": "planner",
    "candidate_nodes": ["search_flight", "collect_passenger", "handoff"]
  },
  "tools": {
    "available": ["flight_search", "ticket_create"],
    "last_results": []
  },
  "governance": {
    "requires_confirmation_tools": ["ticket_create"]
  }
}
```

---

## 7. 工作流与节点模型设计

### 7.1 工作流图执行模式

工作流图仍然是执行边界，但运行时要有“动态路径决策”能力。

#### 7.1.1 节点分类

| 节点类型 | 说明 |
|----------|------|
| `start` | 起始节点 |
| `planner` | 规划节点，决定当前目标和候选路径 |
| `condition` | 规则分支节点 |
| `semantic_branch` | 语义分支节点 |
| `form` | 信息采集节点 |
| `knowledge` | 知识检索节点 |
| `tool` | 外部工具调用节点 |
| `message` | 给用户输出消息 |
| `subflow` | 子流程节点 |
| `handoff` | 转人工或兜底节点 |
| `end` | 结束节点 |

### 7.2 边模型升级

边不应只是简单连线，应具备运行时语义。

#### 7.2.1 Edge 定义建议

```json
{
  "id": "edge_12",
  "source": "planner_1",
  "target": "search_flight",
  "priority": 100,
  "guard": {
    "type": "expression",
    "expression": "departure_city != null and arrival_city != null and departure_date != null"
  },
  "fallback": false
}
```

#### 7.2.2 支持的 guard 类型

- `always`
- `expression`
- `required_fields`
- `semantic_intent`
- `tool_available`
- `risk_approved`

### 7.3 Planner 节点设计

Planner 节点不是普通 LLM 节点，它必须输出结构化结果，并受候选节点列表约束。

#### 7.3.1 Planner 节点输入

- 当前目标
- 当前候选节点
- 缺失字段
- 已完成节点
- 风控状态
- 工作流约束

#### 7.3.2 Planner 节点输出

```json
{
  "selected_node": "search_flight",
  "reason": "用户所需槽位已齐，可直接查询航班",
  "confidence": 0.91,
  "need_user_input": false,
  "missing_fields": []
}
```

### 7.4 Condition 节点设计

`ConditionNode` 负责硬规则判断：

- 必填字段是否缺失
- 数值范围是否满足
- 布尔条件是否通过
- 时间条件是否满足

不建议让 `ConditionNode` 承担复杂语义路由，复杂语义路由由 `semantic_branch` 或 `planner` 处理。

### 7.5 节点跳过机制

为满足“自动判断路径节点执行能力”，必须支持跳过机制。

#### 7.5.1 可跳过场景

- 表单字段已存在且未过期
- 检索缓存仍有效
- 工具结果仍可复用
- 前序节点已经产出满足当前节点所需的结构化数据

#### 7.5.2 跳过记录

节点被跳过时必须记录：

- `node_id`
- `skip_reason`
- `skip_source`
- `context_snapshot_hash`

### 7.6 节点执行日志模型

```json
{
  "execution_id": "exec_001",
  "node_id": "search_flight",
  "node_type": "tool",
  "decision": {
    "selected_by": "planner",
    "reason": "all_required_slots_present",
    "confidence": 0.91
  },
  "status": "completed",
  "input": {
    "departure_city": "上海",
    "arrival_city": "北京",
    "departure_date": "2026-04-21"
  },
  "output": {
    "flight_options": []
  },
  "metrics": {
    "duration_ms": 480
  }
}
```

---

## 8. 通信协议设计

### 8.1 前端与 Java 网关协议

主链路统一采用 `GatewayEnvelope`。

#### 8.1.1 入站消息格式

```json
{
  "type": "action",
  "request_id": "req-001",
  "action": "chat.send",
  "session_id": "sess-001",
  "execution_id": null,
  "payload": {
    "message": "帮我查询明天从上海到北京的机票"
  }
}
```

#### 8.1.2 Ack 消息格式

```json
{
  "type": "ack",
  "request_id": "req-001",
  "action": "chat.send",
  "status": "accepted",
  "data": {
    "execution_id": "exec-1001"
  }
}
```

#### 8.1.3 Event 消息格式

```json
{
  "type": "event",
  "event": "plan.created",
  "session_id": "sess-001",
  "execution_id": "exec-1001",
  "data": {
    "selected_node": "search_flight",
    "reason": "已具备查询条件",
    "confidence": 0.91
  },
  "timestamp": "2026-04-20T10:20:00Z"
}
```

#### 8.1.4 Error 消息格式

```json
{
  "type": "error",
  "request_id": "req-001",
  "code": "unsupported_action",
  "message": "action workflow.publish is not supported",
  "fatal": false
}
```

### 8.2 推荐事件清单

#### 8.2.1 网关事件

- `connection.opened`
- `connection.closed`
- `heartbeat.timeout`

#### 8.2.2 执行事件

- `execution.started`
- `execution.waiting_user`
- `execution.resumed`
- `execution.suspended`
- `execution.completed`
- `execution.failed`

#### 8.2.3 规划事件

- `plan.created`
- `plan.replanned`
- `branch.decided`
- `node.skipped`

#### 8.2.4 节点事件

- `node.started`
- `node.completed`
- `node.failed`
- `tool.called`
- `tool.returned`
- `knowledge.retrieved`
- `message.delta`

### 8.3 Java 与 Python 的内部协议

第一阶段继续采用：

- `HTTP Command`：启动执行、表单提交、恢复执行
- `SSE Event Stream`：执行事件、规划事件、节点事件

#### 8.3.1 保留原因

- 当前仓库已有 `PythonClient` 和 `SSE` 流式能力
- 改造重点应先放在 Netty 主链路和 Planner 内核
- 后续如果并发和可靠性要求继续提升，再升级到 `gRPC bidirectional streaming`

#### 8.3.2 Java 侧桥接要求

`ExecutionEventBridge` 必须：

1. 订阅 Python 的 SSE 事件
2. 将 SSE 事件转成领域事件对象
3. 映射到对应 `session_id / execution_id` 的 Channel
4. 在目标 Channel 的 EventLoop 线程中发回前端

---

## 9. 数据存储设计

### 9.1 现有核心表

- `workflow`
- `workflow_version`
- `session`
- `execution`
- `execution_node_log`
- `audit_log`

### 9.2 新增或增强的数据对象

| 对象 | 作用 |
|------|------|
| `execution_plan_log` | 记录每一次 Planner 输出 |
| `execution_context_snapshot` | 记录关键上下文快照 |
| `session_memory` | 保存可长期复用的用户事实和偏好 |
| `gateway_connection_audit` | 记录连接事件、断开原因、客户端信息 |

### 9.3 execution_plan_log 建议字段

| 字段 | 说明 |
|------|------|
| `execution_id` | 执行实例 ID |
| `plan_round` | 第几轮规划 |
| `current_node_id` | 规划时当前位置 |
| `candidate_nodes` | 候选节点列表 |
| `selected_node` | 最终选中的节点 |
| `reason` | 选路原因摘要 |
| `confidence` | 规划置信度 |
| `need_user_input` | 是否需要用户补充 |
| `planning_context_hash` | 上下文快照哈希 |

### 9.4 Context Snapshot 存储策略

为避免存储膨胀，建议只存以下内容：

- 当前轮用户消息
- 会话摘要
- 关键槽位
- 候选节点列表
- 当前选路结果
- 关键工具结果摘要

不建议把完整原始长对话和超长知识文本每轮都全量落库。

---

## 10. 可观测性与安全设计

### 10.1 网关侧指标

| 指标 | 说明 |
|------|------|
| `gateway_connections_active` | 当前活跃连接数 |
| `gateway_messages_in_total` | 入站消息总量 |
| `gateway_messages_out_total` | 出站消息总量 |
| `gateway_handler_latency_ms` | 各 Handler 延迟 |
| `gateway_protocol_error_total` | 协议错误数 |
| `gateway_backpressure_drop_total` | 背压丢弃数 |

### 10.2 执行侧指标

| 指标 | 说明 |
|------|------|
| `planner_latency_ms` | 单轮规划耗时 |
| `planner_replan_total` | 重规划次数 |
| `node_skip_total` | 节点跳过次数 |
| `branch_decision_total` | 分支判断次数 |
| `tool_call_total` | 工具调用次数 |
| `execution_wait_user_total` | 等待用户次数 |

### 10.3 审计要求

以下内容必须入审计日志：

- 原始 action 信封
- 关键连接事件
- 每一轮 Planner 决策摘要
- 每个节点的输入输出摘要
- 工具调用参数摘要和结果摘要
- 失败异常和堆栈摘要

### 10.4 安全控制

#### 10.4.1 网关安全

- 握手阶段校验 token 和 workspace
- 消息大小限制
- 频率限制
- 非法 action 拒绝
- 高风险动作必须带确认标识

#### 10.4.2 执行安全

- Planner 输出必须是结构化 JSON
- Planner 不能输出工作流图中不存在的节点
- 工具调用必须经过权限和确认检查
- 敏感字段写日志前必须脱敏

### 10.5 失败降级策略

| 场景 | 降级方案 |
|------|----------|
| Planner 置信度过低 | 转入澄清节点或人工节点 |
| 知识检索不可用 | 进入通用问答或提示稍后重试 |
| 工具调用失败 | 重试、降级查询或人工确认 |
| Python 服务超时 | Java 网关发 `execution.failed` 并保留回放信息 |

---

## 11. 落地实施计划

### 11.1 第一阶段：网关主链路重构

目标：把 WebSocket 主链路彻底切到 Netty Handler 架构。

#### 交付项

- 新的 `RobotChannelInitializer`
- `AuthHandshakeHandler`
- `GatewayEnvelopeDecoder`
- `ProtocolValidationHandler`
- `ActionDispatchHandler`
- `GatewayExceptionHandler`
- `ConnectionRegistry`
- 统一 `GatewayEnvelope` 协议对象

#### 完成标准

- 聊天、表单提交、执行恢复都通过 Netty Pipeline
- Spring WebSocket 不再承担主业务交互
- 所有消息都有统一 ack / event / error 返回

### 11.2 第二阶段：执行内核规划化

目标：把 Python 执行引擎升级为“规划驱动执行”。

#### 交付项

- `ContextAssembler`
- `Planner`
- `PathResolver`
- `NodeReadinessChecker`
- `BranchEvaluator`
- `RePlanner`
- `execution_plan_log`

#### 完成标准

- 能根据上下文自动选择候选节点
- 能跳过已满足条件的节点
- 能在表单返回后重规划
- 能记录规划原因和置信度

### 11.3 第三阶段：协议与可观测性完善

目标：让前端、网关、执行内核形成闭环可观测体系。

#### 交付项

- 规划事件和分支事件前端展示
- Replay 视图展示“规划 -> 分支 -> 执行”过程
- 网关和执行指标接入 Prometheus
- Planner 决策审计和快照查询接口

### 11.4 第四阶段：高级能力增强

目标：增强复杂业务场景适应能力。

#### 可选增强项

- `semantic_branch` 节点
- 子流程动态推荐
- `gRPC Streaming` 替换内部 `HTTP + SSE`
- Planner 输出质量评估和离线回放测试

---

## 附录 A：推荐的运行时顺序

### A.1 用户首次发起对话

1. 前端发送 `chat.send`
2. Netty Pipeline 完成解码、校验和会话绑定
3. `ChatActionHandler` 创建执行命令
4. `ExecutionOrchestrator` 创建 `execution`
5. Java 调用 Python 执行内核
6. `ContextAssembler` 装配上下文
7. `Planner` 输出首轮计划
8. `NodeExecutor` 执行节点
9. Java 将事件桥接成 WebSocket `event`
10. 前端更新执行面板和聊天区

### A.2 表单补充后恢复执行

1. 前端发送 `form.submit`
2. `FormSubmitActionHandler` 校验 execution 状态
3. Java 转发到 Python `resume`
4. Python 合并表单数据到上下文
5. `RePlanner` 判断是否需要重规划
6. 若需要则重新选路
7. 继续执行后续节点

### A.3 条件变化导致改路

1. 节点执行后写入新的结构化变量
2. `RePlanner` 发现当前路径条件已改变
3. 重新生成候选节点列表
4. Planner 选择新的合法路径
5. 发出 `plan.replanned` 和 `branch.decided` 事件

---

## 附录 B：本次文档重构后的明确约束

1. Java 后端主交互链路必须是 Netty Handler 架构，不接受“Spring WebSocket 也算 WebSocket”这种宽泛表述。
2. 智能体执行必须经过“上下文装配 -> 规划 -> 路径校验 -> 节点执行 -> 重规划”的闭环，不接受只有静态条件节点的设计。
3. 任何 Planner 输出都必须是结构化、可审计、可回放的。
4. 任何路径选择都必须受工作流图约束，不允许跳出合法节点集合。
5. 任何节点是否执行、是否跳过、为什么跳过，都必须有决策记录。

